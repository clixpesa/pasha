import { tokens } from '../theme/tokens';

export const DEFAULT_BOTTOM_INSET = tokens.space.vl.val;

// Disabling eslint rules for PascalCase enum Member name as IPhoneSE feels wrong
export enum MobileDeviceHeight {
  
  iPhoneSE = 667,
   
  iPhone12 = 812,
}
